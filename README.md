# client-server-multithreaded-chat-system
A multithreaded client-server chat system written in C. The server manages multiple clients simultaneously using threads, receives messages from clients, and routes each message to the appropriate target client. The client application enables sending messages to other connected users and receiving incoming messages from the server in real time.


